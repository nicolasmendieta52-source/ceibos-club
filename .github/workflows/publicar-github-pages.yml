name: Publicar GitHub Pages

on:
  push:
    branches:
      - main
  workflow_run:
    workflows:
      - "Actualizar datos del club"
    types:
      - completed
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  publicar:
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 40
    environment:
      name: github-pages
      url: ${{ steps.publicacion.outputs.page_url || steps.reintento.outputs.page_url }}
    steps:
      - name: Descargar la ultima version
        uses: actions/checkout@v6
        with:
          ref: main

      - name: Configurar GitHub Pages
        uses: actions/configure-pages@v5

      - name: Preparar archivos de la web
        uses: actions/upload-pages-artifact@v4
        with:
          path: .

      - name: Publicar la web
        id: publicacion
        continue-on-error: true
        uses: actions/deploy-pages@v4

      - name: Reintentar la publicacion si GitHub demora
        id: reintento
        if: steps.publicacion.outcome == 'failure'
        uses: actions/deploy-pages@v4
